<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.recycler_view">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.scrollview.NestedScrollViewActivity"></activity>
        <activity android:name=".activity.scrollview.NestedScrollView" />
        <activity android:name=".activity.scrollview.ScrollViewWithRecyclerViewActivity" />
        <activity android:name=".activity.scrollview.CommonScrollViewActivity" />
        <activity android:name=".activity.ScrollViewActivity" />
        <activity android:name=".activity.GridViewActivity" />
        <activity android:name=".activity.RecyclerViewActivity" />
        <activity android:name=".activity.recyclerview.ItemWithButtonActivity" />
        <activity android:name=".activity.recyclerview.ReorderActivity" />
        <activity android:name=".activity.recyclerview.OnClickItemActivity" />
        <activity android:name=".activity.recyclerview.HorizontalRecyclerViewActivity" />
        <activity android:name=".activity.recyclerview.VerticalRecyclerViewActivity" />
        <activity android:name=".activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>